# Backend

This directory contains the Python Flask server for DaraMemo.
It is responsible for:

- Idle time detection logic
- REST API for frontend communication
- Local DB access and user session logging

## Structure

- `app/`: Application logic and API routing
- `tests/`: Unit tests